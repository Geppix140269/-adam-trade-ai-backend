# Port for the API server (Railway will set this automatically)
PORT=3000

# OpenAI API Key (get from https://platform.openai.com/api-keys)
# REQUIRED for the app to work
OPENAI_API_KEY=your_openai_api_key_here

# Node environment
NODE_ENV=production
